你现在要开始实现 Goofspiel-13 项目的 **Local 4060 Smoke Training Milestone**。

这不是最终大规模训练，也不是 MVP，不允许为了方便重新设计模型或替换学习算法。

本阶段的目标非常明确：

> **在一张 RTX 4060（按 8GB VRAM 预算设计）上，把“完整核心模型架构 + 核心学习方法 + 基本训练流程”真正跑通，并产生一个可以保存、加载、评估的 neural checkpoint。**

本阶段验证的是：

1. 模型结构正确；
2. 学习算法正确；
3. 梯度流正确；
4. 数据流正确；
5. 小规模训练确实能够学习；
6. variable-N 设计能工作；
7. 最终能输出真实 checkpoint。

不要求当前模型达到强 N=13 实战水平。

---

# 一、首先阅读规格

开始编码前，读取仓库：

```text
docs/specs/
```

下所有现有设计文档。

尤其必须遵循：

```text
MODEL_SPEC
LEARNING_SPEC
TRAINING_PIPELINE_SPEC
DATA_SPEC
TESTING_AND_OBSERVABILITY_SPEC
ENGINEERING_GUIDE
```

如果不同文档出现实现层细节冲突：

优先级为：

```text
数学/游戏规则不变量
>
专项设计文档
>
工程指南
>
框架默认行为
```

严禁因“更简单”“更常见”“更容易跑”而自行替换已经确定的机制。

---

# 二、本阶段绝对不能删除的模型组件

必须实现并实际进入 forward graph：

```text
Rank Encoder
Card Transformer
Relational GNN
Transformer/GNN Fusion
Joint-Action Pair Builder
Matrix CNN

Robust Q Head
Robust Policy Head
Robust Distributional Value Head

LSTM Current-Game Opponent Encoder
Mamba Cross-Game Opponent Encoder
Opponent Fusion
Short-Term Opponent Prediction Head
Long-Term Opponent Prediction Head
Fused Opponent Prediction Head
Opponent Switch Head

Adaptive Residual Q Branch
Adaptive Policy Head
Adaptive Distributional Head

K=4 lightweight uncertainty/ensemble heads
```

不允许：

```text
删除 GNN
删除 Matrix CNN
用 GRU 替代 LSTM
删除 Mamba
删除 Adaptive branch
把 Q(s,a,b) 改成 Q(s,a)
```

如果某模块当前阶段暂时没有足够训练数据：

可以不重点优化它，

但必须正确实现并通过 forward/unit tests。

---

# 三、模型规模

保持设计规格中的小型完整模型。

目标：

```text
约 6–8M 参数
```

不要因为 4060 做巨幅缩小。

RTX 4060 8GB 完全能够容纳该级别模型。

使用：

```text
PyTorch
CUDA
BF16 autocast where safe
FP32 for Q / probability / Nash calculations
```

暂时不要：

```text
FSDP
Ray
DeepSpeed
Triton
Custom CUDA
```

。

---

# 四、统一 Tensor Shape

即使当前 N < 13：

GPU tensor 仍统一：

```text
Nmax = 13
```

典型：

```text
[B,13,D]
[B,13,13,C]
[B,13,13]
```

使用 legal/valid masks。

禁止为了 N=3 创建完全不同的 model architecture。

---

# 五、本阶段必须实现的核心学习方法

## 1. Robust Q

必须实现：

```text
Full-Matrix Model-Based Nash Bellman Learning
```

对一个 state 的全部合法：

```math
(a,b)
```

构造 target。

不能只训练实际执行的 joint action。

Bellman continuation：

```math
V(s') = NashValue(Q_target(s'))
```

。

使用 EMA target network。

---

## 2. Matrix Nash Solver

先实现：

```text
Reference SciPy FP64 LP
```

再实现：

```text
PyTorch batched Regret Matching+
```

。

GPU solver 必须通过 small random matrix parity test 后才能用于训练。

---

## 3. Robust Actor

必须实现：

```text
NeuRD
```

使用 raw pre-softmax logits。

严禁实现成：

```python
-loss = advantage * log_prob
```

这种普通 policy gradient。

保留 Nash anchor。

---

## 4. Outcome

必须实现：

```text
201-bin Z_R
Monte Carlo terminal target
TD(lambda), lambda=0.90
```

。

MC 不得监督 Q_R。

---

## 5. Opponent Prediction

必须至少真正训练：

```text
q_L
q_M
q_F
```

三个 prediction heads。

实际 opponent action 为监督标签。

不能使用 Nash/Search strategy 当 opponent label。

---

## 6. Adaptive

必须实现 Adaptive branch 与 loss API。

4060 smoke 阶段可以减少更新比例，

但必须至少执行若干真实 adaptive update 并验证：

```text
adaptive backward
不更新 robust backbone
不更新 LSTM/Mamba
```

。

---

# 六、本阶段训练流程

为了快速验证，使用小规模版本：

```text
P0 Verify
↓
P1 Self-Supervised Pretraining Smoke
↓
P3 Exact/Supervised Strategic Warmup
↓
P4 Robust Game-Theoretic RL Smoke
↓
P5 Opponent/Adaptive Smoke
↓
Final Evaluation
↓
Checkpoint
```

本次可以暂时跳过长期：

```text
Semi-supervised large pseudo-label loop
League
Red Team
Reanalysis
Distillation
```

但是不要删除接口或未来目录。

---

# 七、P0 Verification

必须首先通过：

```text
environment tests
bitmask tests
data schema tests
Nash LP tests
GPU RM+ parity
player-swap tests
gradient firewall tests
```

P0 没通过：

禁止开始训练。

---

# 八、P1 Self-Supervised Smoke

生成小型 GameCorpus。

建议：

```text
约 50k–200k states
```

来源：

```text
Random
Parameterized heuristics
Random-vs-Heuristic
```

覆盖：

```text
N=3..13
```

但主要采样：

```text
N=3..7
```

并确保至少：

```text
10%–15%
```

为 N=13。

训练任务至少：

```text
known-transition prediction
immediate joint outcome
player-swap consistency
masked opponent-history action
future opponent action prediction
style contrastive
```

本阶段目标：

不是游戏强度。

而是确认：

```text
transition / immediate task 明显收敛
opponent NLL 优于 uniform
所有模块有正常梯度
```

建议训练：

```text
5k–20k optimizer updates
```

或使用时间上限。

---

# 九、Exact / Strategic Warmup

只使用小 N Exact Teacher：

推荐：

```text
N=3
N=4
N=5
```

如果本机速度允许：

少量 N=6。

生成：

```text
Exact Q
Exact Nash Policy
Exact Value
```

。

进行 Strategic SFT。

要求模型开始显著逼近：

```text
Q*
π*
V*
```

。

不要尝试 Full N=13 Exact。

---

# 十、Robust RL Smoke

主要训练：

```text
N=3
N=5
```

并混入少量：

```text
N=7
N=13
```

。

推荐初始采样：

```text
N=3    35%
N=5    35%
N=7    15%
N=13   15%
```

。

N=13 此时只用于：

```text
variable-N sanity
forward/backward
self-play data generation
```

不要求收敛。

---

# 十一、4060 Smoke Training Budget

目标不是训练满。

建议提供三个 preset：

```text
smoke_30m
smoke_2h
smoke_6h
```

---

## smoke_30m

目的：

```text
确认所有流程可以跑到底并产生 checkpoint
```

---

## smoke_2h

目的：

```text
确认 small-N 真正产生学习信号
```

---

## smoke_6h

目的：

```text
第一次比较完整地观察 architecture + learning + training 是否合理
```

程序必须支持 wall-clock based stopping。

---

# 十二、显存

按：

```text
RTX 4060
8GB VRAM
```

设计。

必须动态 benchmark batch size。

初始可以从：

```text
batch_size=32 or 64
```

开始。

如果安全：

提高到：

```text
128 / 256
```

。

不要硬编码一个巨大 batch。

如果 OOM：

优先：

```text
降低 batch
gradient accumulation
减少 leaf/child batch
```

。

禁止因为 OOM：

```text
删除 Transformer/GNN/Mamba
```

。

---

# 十三、Windows 环境

如果当前机器运行 Windows：

优先使用：

```text
WSL2 + Ubuntu
```

运行训练环境。

特别是：

```text
mamba-ssm
CUDA extensions
PyTorch CUDA tooling
```

优先在 Linux/WSL2 下运行。

不要为了原生 Windows 安装方便自行删除 Mamba。

---

# 十四、这次不要实现的重型能力

本 milestone 暂时不需要真正运行：

```text
GT-CFR large search
SM-MCTS large search
League evolution
Robust/Aggressive/Exploiter population
Red-Team continual loop
Massive Reanalysis
Strong/Fast distillation
Ray distributed cluster
C++ Exact optimization
Triton kernels
CUDA Graph
multi-GPU training
full baseline suite
full release benchmark
```

这些属于 H200 阶段。

注意：

```text
“不运行”
≠
“从设计中删除”
```

。

相关接口应保持兼容。

---

# 十五、必须做的评价

训练结束后自动执行：

## N=3

与 Exact 比：

```text
Q MAE
Value MAE
Policy JSD
Exploitability
```

。

## N=5

同上抽样 Exact states。

## N=13

至少：

```text
vs Random
vs Heuristic Suite
self-play sanity
finite Q/policy
legal policy
```

。

## Opponent

Synthetic held-out opponent：

```text
NLL
Brier
Top1
```

。

---

# 十六、本地模型“成功出来”的定义

只有同时满足以下条件：

```text
[ ] 所有核心 architecture modules 实际存在
[ ] forward/backward 正常
[ ] 没有 NaN/Inf
[ ] Data Schema tests 通过
[ ] Gradient firewall tests 通过
[ ] GPU Nash parity 通过

[ ] self-supervised losses 有明显学习
[ ] Exact/SFT 后 Q error 明显下降
[ ] N=3 robust policy 明显优于 random
[ ] N=3 exploitability 相比初始化明显下降
[ ] opponent predictor 优于 uniform
[ ] adaptive update 可以正确运行

[ ] N=13 可以正常 self-play
[ ] N=13 可以 forward/backward
[ ] checkpoint 能保存
[ ] 新进程能重新加载 checkpoint
[ ] evaluation script 能运行
```

全部满足：

才宣布：

```text
LOCAL_4060_SMOKE_SUCCESS
```

。

---

# 十七、本阶段产物

必须生成：

```text
artifacts/local_4060_smoke/
├── checkpoints/
│   ├── pretrained.pt
│   ├── strategic_sft.pt
│   └── final_smoke.pt
│
├── configs/
│   └── resolved.yaml
│
├── reports/
│   ├── training_summary.md
│   ├── exact_eval.json
│   ├── opponent_eval.json
│   └── smoke_result.json
│
├── logs/
└── metrics/
```

。

---

# 十八、最终 checkpoint 必须包含

```text
model_state
target_model_state
opponent_target_state

optimizer states

training stage
global step

schema version
rules version
feature versions
objective version

full config

RNG state

git commit

evaluation summary
```

。

---

# 十九、最终报告必须明确回答

训练结束后自动生成：

```text
training_summary.md
```

回答：

1. 模型总参数量是多少？
2. 每个主要模块参数量是多少？
3. 最高 VRAM 使用多少？
4. 总运行时间多少？
5. 每阶段运行多少 update？
6. 哪些 loss 明显下降？
7. 哪些没有下降？
8. N=3 Exact Q MAE 多少？
9. N=3 exploitability 是否下降？
10. N=5 如何？
11. N=13 是否稳定运行？
12. opponent NLL 是否优于 uniform？
13. 是否发现 NaN/gradient leakage？
14. 哪些测试失败过？
15. 哪些模块进入 H200 前还需要修？
16. 最终 checkpoint 路径是什么？

---

# 二十、禁止事项

1. 不得为了快速跑通删除已经冻结的模型模块。
2. 不得用 GRU 替代 LSTM。
3. 不得删除 Mamba。
4. 不得删除 GNN/CNN。
5. 不得把 joint Q 改成 single-action Q。
6. 不得把 NeuRD 改成 PPO。
7. 不得用 MC return 训练 Robust Q。
8. 不得只训练 chosen joint action 代替 Full-Matrix target。
9. 不得让 opponent loss 更新 public robust backbone。
10. 不得让 adaptive loss更新 robust/opponent backbone。
11. 不得使用 N=13 Full Exact。
12. 不得因为 4060 慢而删除算法语义。
13. 不得把“训练预算缩小”解释成“架构缩水”。
14. 不得在测试没通过时开始长训练。
15. 不得把 smoke checkpoint 宣传成最终强模型。

---

# 二十一、工程实现原则

这次只缩小：

```text
data amount
training updates
state difficulty distribution
search budget
population size
runtime
```

不缩小：

```text
mathematical semantics
model architecture semantics
loss definitions
gradient isolation
data contracts
```

也就是说：

\[
\boxed{
\textbf{Scale down the experiment, not the idea.}
}
\]

---

# 二十二、最终任务

请按以下顺序工作：

```text
Inspect existing repository
↓
Read all specifications
↓
Create implementation checklist
↓
Implement missing M0–M7 components
↓
Run tests
↓
Fix correctness
↓
Run 30-minute smoke
↓
Inspect metrics
↓
Run 2-hour smoke if healthy
↓
Run optional 6-hour local experiment
↓
Generate final checkpoint
↓
Generate training_summary.md
```

如果遇到设计疑问：

不要自行删除/替换机制。

优先：

1. 查设计文档；
2. 查测试契约；
3. 保留主设计；
4. 将替代方案作为独立 TODO/baseline。

最终目标只有一个：

\[
\boxed{
\textbf{
在单张 RTX 4060 上证明：
这套完整核心架构能够训练，
这些学习方法能够真正产生正确学习信号，
整个训练流程能够走通，
并得到第一个真实可用的模型 checkpoint。
}
}
\]