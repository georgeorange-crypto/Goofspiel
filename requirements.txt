# Goofspiel core (游戏核心)
# (stdlib only — no third-party deps for the env itself)

# Web server
fastapi>=0.100
uvicorn[standard]>=0.23
jinja2>=3.1
pydantic>=2.0
python-multipart>=0.0.6

# Testing
pytest>=7.4

# Exact Nash solver
scipy>=1.10
numpy>=1.24
# Optional: used for preflight RAM availability detection (cross-platform)
psutil>=5.9
